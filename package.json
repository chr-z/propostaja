{
  "name": "propostaja",
  "version": "1.0.0",
  "description": "SaaS de propostas comerciais para freelancers — link para cliente, PDF e Pix copia-e-cola. 100% estático.",
  "private": true,
  "scripts": {
    "test": "node --test tests/",
    "serve": "python -m http.server 8080 || npx --yes serve -l 8080 ."
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
